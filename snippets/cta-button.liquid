<button class="custom-btn {{ class }}" type="{{ type | default: 'button' }}">
  {{ text }}
</button>


<button class="cta-btn {{ class }}" type="{{ type | default: 'button' }}">
    {{ text }}
</button>