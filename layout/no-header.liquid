<!doctype html>
<html lang="{{ request.locale.iso_code }}">
    <head>
        {{ content_for_header }}
    </head>
    <body class="no-header-layout">
        {{ content_for_layout }}
    </body>
</html>
